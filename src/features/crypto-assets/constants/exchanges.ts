// Configuración de exchanges soportados

import { SUPPORTED_EXCHANGES } from "../types";

// Re-exportar para fácil acceso
export { SUPPORTED_EXCHANGES };

// Exchange por defecto
export const DEFAULT_EXCHANGE = "bybit";
